BITRIX_WEBHOOK_URL=https://your-domain.bitrix24.ru/rest/<user_id>/<webhook_token>/
# SECURITY: if a real webhook URL was ever committed, rotate/revoke it in Bitrix24 immediately.
REQUEST_DELAY=0.3
PAGE_SIZE=50
REPORTS_DIR=reports
