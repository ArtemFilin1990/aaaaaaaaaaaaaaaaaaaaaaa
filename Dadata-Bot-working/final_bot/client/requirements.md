## Packages
date-fns | Formatting log timestamps
clsx | Conditional class merging
tailwind-merge | Merging tailwind classes elegantly
lucide-react | Beautiful icons

## Notes
The application requires Google Fonts to be loaded in the index.html or injected via CSS. I will use @import in index.css.
The dashboard relies on the /api/config and /api/logs endpoints as specified in the schema and routes manifest.
